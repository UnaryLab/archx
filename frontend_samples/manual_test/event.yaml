event:
  array_gemm:
    subevent:
    - icnt
    - icmp
    - iadd
    - wcnt
    - wcmp
    - wadd
    - multiplier
    - pe_register
    - accumulator
    - adder
    - ofifo
  attention:
    subevent:
    - qkt
    - av
  av:
    subevent:
    - av_prefill
    - av_decode
  av_decode:
    subevent:
    - input_gemm
    - weight_gemm
    - array_gemm
    - vector_gemm
    - isram_offchip_writes
    - wsram_offchip_writes
    - osram_offchip_writes
    - osram_offchip_reads
    - isram_onchip_reads
    - wsram_onchip_reads
    - osram_onchip_writes
    - osram_onchip_reads
    - dram_input_reads
    - dram_weight_reads
    - dram_output_reads
    - dram_output_writes
  av_prefill:
    subevent:
    - input_gemm
    - weight_gemm
    - array_gemm
    - vector_gemm
    - isram_offchip_writes
    - wsram_offchip_writes
    - osram_offchip_writes
    - osram_offchip_reads
    - isram_onchip_reads
    - wsram_onchip_reads
    - osram_onchip_writes
    - osram_onchip_reads
    - dram_input_reads
    - dram_weight_reads
    - dram_output_reads
    - dram_output_writes
  dram_input_reads:
    subevent:
    - dram
  dram_output_reads:
    subevent:
    - dram
  dram_output_writes:
    subevent:
    - dram
  dram_weight_reads:
    subevent:
    - dram
  ffn:
    subevent:
    - proj_up
    - proj_down
    - proj_gate
  gemm:
    subevent:
    - projection
    - attention
    - ffn
    - output
  gemm_nonlinear:
    subevent:
    - ififo
    - wfifo
    - int_to_fp
    - input_register
    - weight_register
    - int_to_fp
    - icnt
    - icmp
    - iadd
    - wcnt
    - wcmp
    - wadd
    - multiplier
    - pe_register
    - accumulator
    - adder
    - ofifo
  input_gemm:
    subevent:
    - ififo
    - input_register
  isram_offchip_writes:
    subevent:
    - isram
  isram_onchip_reads:
    subevent:
    - isram
  llama_2_13b:
    subevent:
    - gemm
    - nonlinear
  llama_2_70b:
    subevent:
    - gemm
    - nonlinear
  llama_2_70b_GQA:
    subevent:
    - gemm
    - nonlinear
  llama_2_7b:
    subevent:
    - gemm
    - nonlinear
  nonlinear:
    subevent:
    - softmax
    - silu
  osram_offchip_reads:
    subevent:
    - osram
  osram_offchip_writes:
    subevent:
    - osram
  osram_onchip_reads:
    subevent:
    - osram
  osram_onchip_writes:
    subevent:
    - osram
  output:
    subevent:
    - output_prefill
    - output_decode
  output_decode:
    subevent:
    - input_gemm
    - weight_gemm
    - array_gemm
    - vector_gemm
    - isram_offchip_writes
    - wsram_offchip_writes
    - osram_offchip_writes
    - osram_offchip_reads
    - isram_onchip_reads
    - wsram_onchip_reads
    - osram_onchip_writes
    - osram_onchip_reads
    - dram_input_reads
    - dram_weight_reads
    - dram_output_reads
    - dram_output_writes
  output_prefill:
    subevent:
    - input_gemm
    - weight_gemm
    - array_gemm
    - vector_gemm
    - isram_offchip_writes
    - wsram_offchip_writes
    - osram_offchip_writes
    - osram_offchip_reads
    - isram_onchip_reads
    - wsram_onchip_reads
    - osram_onchip_writes
    - osram_onchip_reads
    - dram_input_reads
    - dram_weight_reads
    - dram_output_reads
    - dram_output_writes
  proj_a:
    subevent:
    - proj_a_prefill
    - proj_a_decode
  proj_a_decode:
    subevent:
    - input_gemm
    - weight_gemm
    - array_gemm
    - vector_gemm
    - isram_offchip_writes
    - wsram_offchip_writes
    - osram_offchip_writes
    - osram_offchip_reads
    - isram_onchip_reads
    - wsram_onchip_reads
    - osram_onchip_writes
    - osram_onchip_reads
    - dram_input_reads
    - dram_weight_reads
    - dram_output_reads
    - dram_output_writes
  proj_a_prefill:
    subevent:
    - input_gemm
    - weight_gemm
    - array_gemm
    - vector_gemm
    - isram_offchip_writes
    - wsram_offchip_writes
    - osram_offchip_writes
    - osram_offchip_reads
    - isram_onchip_reads
    - wsram_onchip_reads
    - osram_onchip_writes
    - osram_onchip_reads
    - dram_input_reads
    - dram_weight_reads
    - dram_output_reads
    - dram_output_writes
  proj_down:
    subevent:
    - proj_down_prefill
    - proj_down_decode
  proj_down_decode:
    subevent:
    - input_gemm
    - weight_gemm
    - array_gemm
    - vector_gemm
    - isram_offchip_writes
    - wsram_offchip_writes
    - osram_offchip_writes
    - osram_offchip_reads
    - isram_onchip_reads
    - wsram_onchip_reads
    - osram_onchip_writes
    - osram_onchip_reads
    - dram_input_reads
    - dram_weight_reads
    - dram_output_reads
    - dram_output_writes
  proj_down_prefill:
    subevent:
    - input_gemm
    - weight_gemm
    - array_gemm
    - vector_gemm
    - isram_offchip_writes
    - wsram_offchip_writes
    - osram_offchip_writes
    - osram_offchip_reads
    - isram_onchip_reads
    - wsram_onchip_reads
    - osram_onchip_writes
    - osram_onchip_reads
    - dram_input_reads
    - dram_weight_reads
    - dram_output_reads
    - dram_output_writes
  proj_gate:
    subevent:
    - proj_gate_prefill
    - proj_gate_decode
  proj_gate_decode:
    subevent:
    - input_gemm
    - weight_gemm
    - array_gemm
    - vector_gemm
    - isram_offchip_writes
    - wsram_offchip_writes
    - osram_offchip_writes
    - osram_offchip_reads
    - isram_onchip_reads
    - wsram_onchip_reads
    - osram_onchip_writes
    - osram_onchip_reads
    - dram_input_reads
    - dram_weight_reads
    - dram_output_reads
    - dram_output_writes
  proj_gate_prefill:
    subevent:
    - input_gemm
    - weight_gemm
    - array_gemm
    - vector_gemm
    - isram_offchip_writes
    - wsram_offchip_writes
    - osram_offchip_writes
    - osram_offchip_reads
    - isram_onchip_reads
    - wsram_onchip_reads
    - osram_onchip_writes
    - osram_onchip_reads
    - dram_input_reads
    - dram_weight_reads
    - dram_output_reads
    - dram_output_writes
  proj_k:
    subevent:
    - proj_k_prefill
    - proj_k_decode
  proj_k_decode:
    subevent:
    - input_gemm
    - weight_gemm
    - array_gemm
    - vector_gemm
    - isram_offchip_writes
    - wsram_offchip_writes
    - osram_offchip_writes
    - osram_offchip_reads
    - isram_onchip_reads
    - wsram_onchip_reads
    - osram_onchip_writes
    - osram_onchip_reads
    - dram_input_reads
    - dram_weight_reads
    - dram_output_reads
    - dram_output_writes
  proj_k_prefill:
    subevent:
    - input_gemm
    - weight_gemm
    - array_gemm
    - vector_gemm
    - isram_offchip_writes
    - wsram_offchip_writes
    - osram_offchip_writes
    - osram_offchip_reads
    - isram_onchip_reads
    - wsram_onchip_reads
    - osram_onchip_writes
    - osram_onchip_reads
    - dram_input_reads
    - dram_weight_reads
    - dram_output_reads
    - dram_output_writes
  proj_q:
    subevent:
    - proj_q_prefill
    - proj_q_decode
  proj_q_decode:
    subevent:
    - input_gemm
    - weight_gemm
    - array_gemm
    - vector_gemm
    - isram_offchip_writes
    - wsram_offchip_writes
    - osram_offchip_writes
    - osram_offchip_reads
    - isram_onchip_reads
    - wsram_onchip_reads
    - osram_onchip_writes
    - osram_onchip_reads
    - dram_input_reads
    - dram_weight_reads
    - dram_output_reads
    - dram_output_writes
  proj_q_prefill:
    subevent:
    - input_gemm
    - weight_gemm
    - array_gemm
    - vector_gemm
    - isram_offchip_writes
    - wsram_offchip_writes
    - osram_offchip_writes
    - osram_offchip_reads
    - isram_onchip_reads
    - wsram_onchip_reads
    - osram_onchip_writes
    - osram_onchip_reads
    - dram_input_reads
    - dram_weight_reads
    - dram_output_reads
    - dram_output_writes
  proj_up:
    subevent:
    - proj_up_prefill
    - proj_up_decode
  proj_up_decode:
    subevent:
    - input_gemm
    - weight_gemm
    - array_gemm
    - vector_gemm
    - isram_offchip_writes
    - wsram_offchip_writes
    - osram_offchip_writes
    - osram_offchip_reads
    - isram_onchip_reads
    - wsram_onchip_reads
    - osram_onchip_writes
    - osram_onchip_reads
    - dram_input_reads
    - dram_weight_reads
    - dram_output_reads
    - dram_output_writes
  proj_up_prefill:
    subevent:
    - input_gemm
    - weight_gemm
    - array_gemm
    - vector_gemm
    - isram_offchip_writes
    - wsram_offchip_writes
    - osram_offchip_writes
    - osram_offchip_reads
    - isram_onchip_reads
    - wsram_onchip_reads
    - osram_onchip_writes
    - osram_onchip_reads
    - dram_input_reads
    - dram_weight_reads
    - dram_output_reads
    - dram_output_writes
  proj_v:
    subevent:
    - proj_v_prefill
    - proj_v_decode
  proj_v_decode:
    subevent:
    - input_gemm
    - weight_gemm
    - array_gemm
    - vector_gemm
    - isram_offchip_writes
    - wsram_offchip_writes
    - osram_offchip_writes
    - osram_offchip_reads
    - isram_onchip_reads
    - wsram_onchip_reads
    - osram_onchip_writes
    - osram_onchip_reads
    - dram_input_reads
    - dram_weight_reads
    - dram_output_reads
    - dram_output_writes
  proj_v_prefill:
    subevent:
    - input_gemm
    - weight_gemm
    - array_gemm
    - vector_gemm
    - isram_offchip_writes
    - wsram_offchip_writes
    - osram_offchip_writes
    - osram_offchip_reads
    - isram_onchip_reads
    - wsram_onchip_reads
    - osram_onchip_writes
    - osram_onchip_reads
    - dram_input_reads
    - dram_weight_reads
    - dram_output_reads
    - dram_output_writes
  projection:
    subevent:
    - proj_q
    - proj_k
    - proj_v
    - proj_a
  qkt:
    subevent:
    - qkt_prefill
    - qkt_decode
  qkt_decode:
    subevent:
    - input_gemm
    - weight_gemm
    - array_gemm
    - vector_gemm
    - isram_offchip_writes
    - wsram_offchip_writes
    - osram_offchip_writes
    - osram_offchip_reads
    - isram_onchip_reads
    - wsram_onchip_reads
    - osram_onchip_writes
    - osram_onchip_reads
    - dram_input_reads
    - dram_weight_reads
    - dram_output_reads
    - dram_output_writes
  qkt_prefill:
    subevent:
    - input_gemm
    - weight_gemm
    - array_gemm
    - vector_gemm
    - isram_offchip_writes
    - wsram_offchip_writes
    - osram_offchip_writes
    - osram_offchip_reads
    - isram_onchip_reads
    - wsram_onchip_reads
    - osram_onchip_writes
    - osram_onchip_reads
    - dram_input_reads
    - dram_weight_reads
    - dram_output_reads
    - dram_output_writes
  silu:
    subevent:
    - silu_prefill
    - silu_decode
  silu_decode:
    subevent:
    - gemm_nonlinear
    - silu_nonlinear
    - isram_offchip_writes
    - wsram_offchip_writes
    - osram_offchip_writes
    - osram_offchip_reads
    - isram_onchip_reads
    - wsram_onchip_reads
    - osram_onchip_writes
    - osram_onchip_reads
    - dram_input_reads
    - dram_weight_reads
    - dram_output_reads
    - dram_output_writes
  silu_nonlinear:
    subevent:
    - multiplier_vector
    - accumulator_vector
    - mac_register_vector
    - register_vector
  silu_prefill:
    subevent:
    - gemm_nonlinear
    - silu_nonlinear
    - isram_offchip_writes
    - wsram_offchip_writes
    - osram_offchip_writes
    - osram_offchip_reads
    - isram_onchip_reads
    - wsram_onchip_reads
    - osram_onchip_writes
    - osram_onchip_reads
    - dram_input_reads
    - dram_weight_reads
    - dram_output_reads
    - dram_output_writes
  softmax:
    subevent:
    - softmax_prefill
    - softmax_decode
  softmax_decode:
    subevent:
    - gemm_nonlinear
    - softmax_nonlinear
    - isram_offchip_writes
    - wsram_offchip_writes
    - osram_offchip_writes
    - osram_offchip_reads
    - isram_onchip_reads
    - wsram_onchip_reads
    - osram_onchip_writes
    - osram_onchip_reads
    - dram_input_reads
    - dram_weight_reads
    - dram_output_reads
    - dram_output_writes
  softmax_nonlinear:
    subevent:
    - multiplier_vector
    - accumulator_vector
    - mac_register_vector
    - register_vector
  softmax_prefill:
    subevent:
    - gemm_nonlinear
    - softmax_nonlinear
    - isram_offchip_writes
    - wsram_offchip_writes
    - osram_offchip_writes
    - osram_offchip_reads
    - isram_onchip_reads
    - wsram_onchip_reads
    - osram_onchip_writes
    - osram_onchip_reads
    - dram_input_reads
    - dram_weight_reads
    - dram_output_reads
    - dram_output_writes
  vector_gemm:
    subevent:
    - multiplier_vector
    - accumulator_vector
    - mac_register_vector
    - register_vector
  weight_gemm:
    subevent:
    - wfifo
    - weight_register
    - int_to_fp
  wsram_offchip_writes:
    subevent:
    - wsram
  wsram_onchip_reads:
    subevent:
    - wsram
