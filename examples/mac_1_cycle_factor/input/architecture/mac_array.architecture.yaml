architecture: # architecture is a flattened description of all components
  module:
    multiplier:
      tag: [compute, onchip]
      instance: [8, 1]
      query:
        class: multiplier
        width: 8
    adder:
      tag: [compute, onchip]
      instance: [8, 1]
      query:
        class: adder
        width: 32