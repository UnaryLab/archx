Agraph:
    architecture: node
    event:        node
    metric:       node
    workload:     node
    constraint:   edge

  step 1. Add nodes (architecture, event, metric, workload)
  step 2. Add edges (constraint)
  step 3. Compute constraints
  step 4. Split nodes
    - add anti constraints to same parameters
  step 5. Create subgraphs
step 6. Process subgraph sweeping
step 7. Process global sweeping